FROM python:3
ARG DB_NAME="your_db_name"
ARG UPDATE_INTERVAL="3600"
ARG WEATHER_LOCATION="your_weather_location"

ADD your_backend_app_filename.py /
RUN pip install pymongo requests

ENV DB_NAME=${DB_NAME}
ENV UPDATE_INTERVAL=${UPDATE_INTERVAL}
ENV WEATHER_LOCATION=${WEATHER_LOCATION}

ENTRYPOINT ["python"]
CMD ["newbackend.py"]
